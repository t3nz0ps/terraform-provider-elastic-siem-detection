## 0.0.10

- Examples and docs now use YAML files for rule/exception definitions
- Added support for `investigation_fields` in detection rules
- Added `new_terms` rule type with `history_window_start` and `new_terms_fields` support

## 0.0.9

- See [tsigouris007](https://github.com/tsigouris007/terraform-provider-elastic-siem-detection)
